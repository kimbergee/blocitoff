<!DOCTYPE html>
<html>
<head>
  <link href="https://maxcdn.bootstrapcdn.com/bootswatch/3.3.6/yeti/bootstrap.min.css" rel="stylesheet" integrity="sha384-yxFy3Tt84CcGRj9UI7RA25hoUMpUPoFzcdPtK3hBdNgEGnh9FdKgMVM+lbAZTKN2" crossorigin="anonymous">
  <title>Blocitoff</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <link href="https://fonts.googleapis.com/css?family=Arima+Madurai|Indie+Flower" rel="stylesheet">

</head>
<body>

  <div class="container">
    <div class="navbar">
    <ul class="nav nav-tabs" id="nav-barline">
      <li><%= link_to "home", welcome_index_path, :class => 'navbuttons' %></li>
      <li><%= link_to "about", welcome_about_path, :class => 'navbuttons' %></li>
      <div class="pull-right user-info">
        <% if user_signed_in? %>
          <p id="white">Signed in as: <strong><%= link_to current_user.username.capitalize, users_show_path, :class => 'link' %></strong> |
            <%= link_to 'Edit profile', edit_user_registration_path, :class => 'link' %> - <%= link_to "Sign Out", destroy_user_session_path, method: :delete, :class => 'link'  %>
          <% else %>
            <%= link_to "sign up", new_user_registration_path, :class => 'link'  %> -
            <%= link_to "sign in", new_user_session_path, :class => 'link'  %>
          </p>
        <% end %>
      </div>
    </ul>
  </div>
    <% flash.each do |key, value| %>
      <div class="alert <%= alert_class_for(key) %> alert-dismissible fade in">
        <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
        <ul>
          <li>
            <%= value %>
          </li>
        </ul>
      </div>
    <% end %>
    <%= yield %>
  </div>


</body>
</html>
